# AGENTS.md

This is a solo-maintainer scholarly typesetting template. Preserve deterministic
source packaging, fail-closed validation, pinned build actions, and explicit
publication gates.

Run the repository's required gate before proposing a change. Do not introduce
mandatory reviewers, CODEOWNERS approval, team gates, LaTeX shell escape,
floating actions, or automatic publication from an ordinary branch push.
