# Security

Use GitHub private vulnerability reporting. Treat LaTeX/Typst packages, fonts,
images, bibliography records, generated source bundles, and publication
credentials as supply-chain inputs. Shell escape is disabled unless a narrowly
documented repository contract requires it.
