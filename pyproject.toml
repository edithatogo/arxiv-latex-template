[project]
name = "arxiv-latex-template"
version = "0.1.0"
description = "Reusable arXiv-ready LaTeX manuscript template and packaging workflow."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

